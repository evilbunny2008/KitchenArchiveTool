org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# Opted out of AGP 9's built-in Kotlin support (see root build.gradle.kts
# comment) -- testing whether Data Binding's Kotlin @BindingAdapter
# resolution needs the traditional org.jetbrains.kotlin.android plugin's
# Java-stub generation, which built-in Kotlin may not yet fully replicate.
android.builtInKotlin=false
